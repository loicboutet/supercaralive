<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #f59e0b;
        color: white;
        padding: 20px;
        text-align: center;
        border-radius: 8px 8px 0 0;
      }
      .content {
        background-color: #ffffff;
        padding: 30px;
        border: 1px solid #e5e7eb;
        border-top: none;
      }
      .alert-box {
        background-color: #fef3c7;
        border-left: 4px solid #f59e0b;
        padding: 15px;
        border-radius: 6px;
        margin: 20px 0;
      }
      .booking-info {
        background-color: #f9fafb;
        padding: 15px;
        border-radius: 6px;
        margin: 20px 0;
        border-left: 4px solid #f59e0b;
      }
      .button {
        display: inline-block;
        background-color: #f59e0b;
        color: white;
        padding: 12px 24px;
        text-decoration: none;
        border-radius: 6px;
        margin: 20px 0;
      }
      .footer {
        text-align: center;
        padding: 20px;
        color: #6b7280;
        font-size: 12px;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>Rappel de rendez-vous</h1>
    </div>
    
    <div class="content">
      <p>Bonjour <%= @professional.first_name.present? ? @professional.first_name : @professional.email %>,</p>
      
      <div class="alert-box">
        <p><strong>⏰ Rappel important</strong></p>
        <p>Vous avez un rendez-vous prévu dans <strong>1 jour ou moins</strong>.</p>
      </div>
      
      <div class="booking-info">
        <p><strong>Service :</strong> <%= @professional_service.name %></p>
        <% if @client.present? %>
          <p><strong>Client :</strong> <%= @client.display_complete_name? ? @client.get_full_name : @client.pseudonym %></p>
        <% end %>
        <% if @vehicle.present? %>
          <p><strong>Véhicule :</strong> <%= @vehicle.brand %> <%= @vehicle.model %> (<%= @vehicle.year %>)</p>
        <% end %>
        <% if @booking.scheduled_at.present? %>
          <p><strong>Date et heure :</strong> <%= @booking.scheduled_at.strftime("%d/%m/%Y à %H:%M") %></p>
          <% days_until = ((@booking.scheduled_at.to_date - Date.current).to_i) %>
          <p><strong>Dans :</strong> <%= days_until %> jour<%= days_until > 1 ? 's' : '' %></p>
        <% end %>
        <% if @booking.estimated_price.present? %>
          <p><strong>Prix estimé :</strong> <%= number_to_currency(@booking.estimated_price, unit: "€", separator: ",", delimiter: " ") %></p>
        <% end %>
        <p><strong>Statut :</strong> <%= @booking.status.humanize %></p>
      </div>
      
      <p>N'oubliez pas de préparer votre intervention et de confirmer votre disponibilité avec le client si nécessaire.</p>
      
      <p style="text-align: center;">
        <a href="<%= professional_booking_url(@booking) %>" class="button">Voir la réservation</a>
      </p>
      
      <p>Cordialement,<br>L'équipe SuperCarAlive</p>
    </div>
    
    <div class="footer">
      <p>© <%= Date.current.year %> SuperCarAlive. Tous droits réservés.</p>
    </div>
  </body>
</html>

