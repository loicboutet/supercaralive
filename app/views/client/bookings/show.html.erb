<div class="container mx-auto px-6 py-8">
  <div class="max-w-4xl mx-auto">
    <div class="mb-8">
      <%= link_to client_bookings_path, class: "text-blue-hero hover:underline inline-flex items-center mb-4" do %>
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        Retour aux réservations
      <% end %>
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-4xl font-bold text-gray-900 mb-2">Détails de la réservation</h1>
          <p class="text-gray-600">Réservation #<%= @booking&.id || '12345' %></p>
        </div>
        <span class="px-4 py-2 rounded-friendly text-sm font-bold <%= @booking&.status == 'pending' ? 'bg-yellow-100 text-yellow-800' : @booking&.status == 'confirmed' ? 'bg-blue-100 text-blue-800' : @booking&.status == 'completed' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
          <%= @booking&.status == 'pending' ? 'En attente de validation' : @booking&.status == 'confirmed' ? 'Confirmée' : @booking&.status == 'completed' ? 'Terminée' : 'Annulée' %>
        </span>
      </div>
    </div>

    <div class="grid lg:grid-cols-3 gap-6">
      <!-- Main Content -->
      <div class="lg:col-span-2 space-y-6">
        <!-- Service Details -->
        <div class="bg-white rounded-super p-6 shadow-sm">
          <h2 class="text-2xl font-bold text-gray-900 mb-6">Informations du service</h2>
          
          <div class="space-y-4">
            <div class="flex items-start border-b border-gray-100 pb-4">
              <svg class="w-6 h-6 text-gray-400 mr-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
              </svg>
              <div class="flex-1">
                <p class="text-sm text-gray-600">Type de service</p>
                <p class="text-lg font-bold text-gray-900"><%= @booking&.service_type || 'Entretien annuel' %></p>
              </div>
            </div>

            <div class="flex items-start border-b border-gray-100 pb-4">
              <svg class="w-6 h-6 text-gray-400 mr-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
              <div class="flex-1">
                <p class="text-sm text-gray-600">Date et heure</p>
                <p class="text-lg font-bold text-gray-900"><%= @booking&.scheduled_at&.strftime("%d/%m/%Y à %H:%M") || 'À définir' %></p>
              </div>
            </div>

            <div class="flex items-start border-b border-gray-100 pb-4">
              <svg class="w-6 h-6 text-gray-400 mr-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <div class="flex-1">
                <p class="text-sm text-gray-600">Véhicule</p>
                <p class="text-lg font-bold text-gray-900"><%= @booking&.vehicle_model || 'Peugeot 208' %></p>
                <p class="text-sm text-gray-500">Kilométrage: <%= number_with_delimiter(@booking&.current_mileage || 50000) %> km</p>
              </div>
            </div>

            <% if @booking&.description.present? %>
              <div class="flex items-start">
                <svg class="w-6 h-6 text-gray-400 mr-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                </svg>
                <div class="flex-1">
                  <p class="text-sm text-gray-600 mb-2">Description</p>
                  <div class="bg-soft p-4 rounded-friendly">
                    <p class="text-gray-700"><%= @booking.description %></p>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>

        <!-- Messages -->
        <div class="bg-white rounded-super p-6 shadow-sm">
          <div class="flex items-center justify-between mb-6">
            <h2 class="text-2xl font-bold text-gray-900">Messages</h2>
            <span class="text-sm text-gray-500">Communication avec le professionnel</span>
          </div>

          <div class="space-y-4 mb-6 max-h-96 overflow-y-auto">
            <!-- Sample messages -->
            <div class="flex items-start space-x-3">
              <div class="w-10 h-10 bg-blue-hero rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-white font-bold">P</span>
              </div>
              <div class="flex-1 bg-soft rounded-friendly p-4">
                <p class="font-medium text-gray-900 mb-1">Professionnel</p>
                <p class="text-gray-700 text-sm">Bonjour, j'ai bien reçu votre demande. Je peux confirmer le créneau proposé.</p>
                <p class="text-xs text-gray-500 mt-2">Il y a 2 heures</p>
              </div>
            </div>

            <div class="flex items-start space-x-3 justify-end">
              <div class="flex-1 bg-red-hero bg-opacity-10 rounded-friendly p-4 text-right">
                <p class="font-medium text-gray-900 mb-1">Vous</p>
                <p class="text-gray-700 text-sm">Parfait, merci ! À demain.</p>
                <p class="text-xs text-gray-500 mt-2">Il y a 1 heure</p>
              </div>
              <div class="w-10 h-10 bg-red-hero rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-white font-bold">V</span>
              </div>
            </div>
          </div>

          <%= form_with url: client_booking_messages_path(@booking), class: "flex space-x-2" do |f| %>
            <%= f.text_field :content, class: "flex-1 px-4 py-3 border border-gray-300 rounded-friendly focus:outline-none focus:ring-2 focus:ring-red-hero focus:border-transparent", placeholder: "Écrivez votre message..." %>
            <%= f.submit "Envoyer", class: "bg-red-hero hover:bg-red-dark text-white px-6 py-3 rounded-friendly font-bold cursor-pointer" %>
          <% end %>
        </div>
      </div>

      <!-- Sidebar -->
      <div class="space-y-6">
        <!-- Professional Info -->
        <div class="bg-white rounded-super p-6 shadow-sm">
          <h3 class="text-lg font-bold text-gray-900 mb-4">Professionnel</h3>
          <div class="flex items-center space-x-3 mb-4">
            <div class="w-12 h-12 bg-blue-hero rounded-friendly flex items-center justify-center">
              <span class="text-white text-xl">🔧</span>
            </div>
            <div>
              <p class="font-bold text-gray-900"><%= @booking&.professional_name || 'AutoPro Garage' %></p>
              <div class="flex items-center text-sm text-yellow-500">
                ★★★★★ <span class="text-gray-600 ml-1">(4.8)</span>
              </div>
            </div>
          </div>
          <%= link_to "Voir le profil", client_professional_path(@booking&.professional_id || 1), class: "block w-full bg-blue-hero hover:bg-blue-dark text-white px-4 py-2 rounded-friendly font-medium text-center text-sm" %>
        </div>

        <!-- Pricing -->
        <div class="bg-white rounded-super p-6 shadow-sm">
          <h3 class="text-lg font-bold text-gray-900 mb-4">Tarification</h3>
          <% if @booking&.price.present? %>
            <div class="space-y-2 mb-4">
              <div class="flex justify-between text-gray-600">
                <span>Service</span>
                <span><%= number_to_currency(@booking.price, unit: "€", separator: ",", delimiter: " ", format: "%n %u") %></span>
              </div>
              <% if @booking.travel_fee.present? %>
                <div class="flex justify-between text-gray-600">
                  <span>Frais de déplacement</span>
                  <span><%= number_to_currency(@booking.travel_fee, unit: "€", separator: ",", delimiter: " ", format: "%n %u") %></span>
                </div>
              <% end %>
              <div class="border-t border-gray-200 pt-2 flex justify-between font-bold text-lg">
                <span>Total</span>
                <span><%= number_to_currency((@booking.price.to_f + (@booking.travel_fee.to_f || 0)), unit: "€", separator: ",", delimiter: " ", format: "%n %u") %></span>
              </div>
            </div>
            <% if @booking.status == 'confirmed' && !@booking.paid? %>
              <%= link_to "Procéder au paiement", client_payment_path(@booking), class: "block w-full bg-red-hero hover:bg-red-dark text-white px-4 py-3 rounded-friendly font-bold text-center" %>
            <% elsif @booking.paid? %>
              <div class="bg-green-50 border-l-4 border-green-500 p-4 rounded-friendly">
                <p class="text-sm font-bold text-green-800">✓ Payé</p>
              </div>
            <% end %>
          <% else %>
            <p class="text-gray-500 text-sm">Le prix sera défini par le professionnel après validation</p>
          <% end %>
        </div>

        <!-- Actions -->
        <div class="bg-white rounded-super p-6 shadow-sm space-y-2">
          <% if @booking&.status == 'pending' %>
            <%= link_to edit_client_booking_path(@booking), class: "block w-full bg-yellow-hero hover:bg-opacity-90 text-gray-900 px-4 py-3 rounded-friendly font-bold text-center" do %>
              Modifier la réservation
            <% end %>
            <%= button_to cancel_client_booking_path(@booking), method: :patch, data: { confirm: "Êtes-vous sûr de vouloir annuler cette réservation ?" }, class: "w-full bg-gray-200 hover:bg-gray-300 text-gray-700 px-4 py-3 rounded-friendly font-bold" do %>
              Annuler la réservation
            <% end %>
          <% elsif @booking&.status == 'completed' && !@booking.reviewed? %>
            <%= link_to new_client_review_path(booking_id: @booking.id), class: "block w-full bg-green-500 hover:bg-green-600 text-white px-4 py-3 rounded-friendly font-bold text-center" do %>
              Laisser un avis
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
