<div class="container mx-auto px-6 py-8">
  <div class="max-w-3xl mx-auto">
    <div class="mb-8">
      <%= link_to client_booking_path(params[:booking_id] || 1), class: "text-blue-hero hover:underline inline-flex items-center mb-4" do %>
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        Retour à la réservation
      <% end %>
      <h1 class="text-4xl font-bold text-gray-900 mb-2">Laisser un avis</h1>
      <p class="text-gray-600">Partagez votre expérience avec le professionnel</p>
    </div>

    <div class="bg-white rounded-super p-8 shadow-sm">
      <%= form_with url: client_reviews_path, method: :post, class: "space-y-6" do |f| %>
        
        <%= f.hidden_field :booking_id, value: params[:booking_id] %>

        <% if @review&.errors&.any? %>
          <div class="bg-red-50 border-l-4 border-red-hero rounded-lg p-4">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-red-hero" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                </svg>
              </div>
              <div class="ml-3">
                <h3 class="text-sm font-bold text-red-800">Erreurs détectées :</h3>
                <ul class="mt-2 text-sm text-red-700 list-disc list-inside">
                  <% @review.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          </div>
        <% end %>

        <!-- Service Info -->
        <div class="bg-soft p-6 rounded-lg">
          <h3 class="font-bold text-gray-900 mb-2">Service évalué</h3>
          <p class="text-gray-700">Entretien annuel - AutoPro Garage</p>
          <p class="text-sm text-gray-600">Réalisé le <%= Date.current.strftime("%d/%m/%Y") %></p>
        </div>

        <!-- Rating Criteria -->
        <div>
          <h3 class="text-lg font-bold text-gray-900 mb-4">Évaluez le service</h3>
          <p class="text-sm text-gray-600 mb-6">Notez chaque critère de 1 à 5 étoiles</p>

          <div class="space-y-6">
            <!-- Punctuality -->
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">
                ⏰ Ponctualité
              </label>
              <div class="flex items-center space-x-2">
                <div class="flex space-x-1">
                  <% 5.times do |i| %>
                    <button type="button" class="star-btn text-3xl text-gray-300 hover:text-yellow-500 transition" data-rating="<%= i + 1 %>" data-field="punctuality">
                      ★
                    </button>
                  <% end %>
                </div>
                <%= f.hidden_field :punctuality_rating, value: 0 %>
              </div>
            </div>

            <!-- Quality -->
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">
                ✨ Qualité du travail
              </label>
              <div class="flex items-center space-x-2">
                <div class="flex space-x-1">
                  <% 5.times do |i| %>
                    <button type="button" class="star-btn text-3xl text-gray-300 hover:text-yellow-500 transition" data-rating="<%= i + 1 %>" data-field="quality">
                      ★
                    </button>
                  <% end %>
                </div>
                <%= f.hidden_field :quality_rating, value: 0 %>
              </div>
            </div>

            <!-- Cleanliness -->
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">
                🧹 Propreté
              </label>
              <div class="flex items-center space-x-2">
                <div class="flex space-x-1">
                  <% 5.times do |i| %>
                    <button type="button" class="star-btn text-3xl text-gray-300 hover:text-yellow-500 transition" data-rating="<%= i + 1 %>" data-field="cleanliness">
                      ★
                    </button>
                  <% end %>
                </div>
                <%= f.hidden_field :cleanliness_rating, value: 0 %>
              </div>
            </div>

            <!-- Interpersonal Skills -->
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">
                👤 Relationnel
              </label>
              <div class="flex items-center space-x-2">
                <div class="flex space-x-1">
                  <% 5.times do |i| %>
                    <button type="button" class="star-btn text-3xl text-gray-300 hover:text-yellow-500 transition" data-rating="<%= i + 1 %>" data-field="interpersonal">
                      ★
                    </button>
                  <% end %>
                </div>
                <%= f.hidden_field :interpersonal_rating, value: 0 %>
              </div>
            </div>

            <!-- Value for Money -->
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">
                💰 Rapport qualité/prix
              </label>
              <div class="flex items-center space-x-2">
                <div class="flex space-x-1">
                  <% 5.times do |i| %>
                    <button type="button" class="star-btn text-3xl text-gray-300 hover:text-yellow-500 transition" data-rating="<%= i + 1 %>" data-field="value">
                      ★
                    </button>
                  <% end %>
                </div>
                <%= f.hidden_field :value_rating, value: 0 %>
              </div>
            </div>
          </div>
        </div>

        <!-- Comment -->
        <div>
          <%= f.label :comment, "Votre commentaire", class: "block text-sm font-medium text-gray-700 mb-2" %>
          <%= f.text_area :comment, rows: 6, class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-hero focus:border-transparent", placeholder: "Décrivez votre expérience avec ce professionnel..." %>
          <p class="text-sm text-gray-500 mt-1">Votre avis aidera d'autres clients à faire leur choix</p>
        </div>

        <!-- Info Banner -->
        <div class="bg-blue-50 border-l-4 border-blue-hero rounded-lg p-4">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-blue-hero" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
              </svg>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-bold text-blue-800">Avis public</h3>
              <p class="text-sm text-blue-700 mt-1">
                Votre avis sera visible publiquement sur le profil du professionnel. Seul votre pseudonyme sera affiché.
              </p>
            </div>
          </div>
        </div>

        <div class="flex space-x-4 pt-4">
          <%= f.submit "Publier mon avis", class: "flex-1 bg-red-hero hover:bg-red-dark text-white px-8 py-3 rounded-lg font-bold cursor-pointer" %>
          <%= link_to "Annuler", client_booking_path(params[:booking_id] || 1), class: "flex-1 bg-gray-200 hover:bg-gray-300 text-gray-700 px-8 py-3 rounded-lg font-bold text-center" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

<script>
  // Simple star rating functionality
  document.querySelectorAll('.star-btn').forEach(btn => {
    btn.addEventListener('click', function(e) {
      e.preventDefault();
      const rating = parseInt(this.dataset.rating);
      const field = this.dataset.field;
      const container = this.parentElement;
      const hiddenField = container.parentElement.querySelector(`input[id*="${field}"]`);
      
      if (hiddenField) {
        hiddenField.value = rating;
      }
      
      // Update visual feedback
      container.querySelectorAll('.star-btn').forEach((star, index) => {
        if (index < rating) {
          star.classList.remove('text-gray-300');
          star.classList.add('text-yellow-500');
        } else {
          star.classList.remove('text-yellow-500');
          star.classList.add('text-gray-300');
        }
      });
    });
  });
</script>
