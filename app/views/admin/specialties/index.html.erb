<% content_for :page_title, "Gestion des Spécialités" %>

<!-- Page Header -->
<div class="mb-8">
  <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-2">
    Gestion des <span class="text-blue-hero">Spécialités</span> 🔧
  </h1>
  <p class="text-xl text-gray-600">
    Gérez les spécialités proposées sur la plateforme
  </p>
</div>

<!-- Stats Cards -->
<div class="grid md:grid-cols-1 gap-6 mb-8">
  <div class="bg-white rounded-lg p-8 shadow-sm">
    <div class="flex items-center justify-between">
      <div class="flex flex-col">
        <span class="text-lg font-bold text-gray-700 mb-2">Total</span>
        <p class="text-5xl font-bold text-blue-hero mb-2"><%= @total_count %></p>
        <p class="text-base text-gray-500">Spécialités</p>
      </div>
      <div class="w-16 h-16 bg-blue-hero rounded-lg flex items-center justify-center flex-shrink-0">
        <span class="text-white text-3xl">🔧</span>
      </div>
    </div>
  </div>
</div>

<!-- Filters -->
<%= form_with url: admin_specialties_path, method: :get, local: true, data: { turbo: false }, class: "bg-white rounded-super p-6 shadow-sm mb-6" do |f| %>
  <div class="flex flex-col md:flex-row gap-4">
    <div class="flex-1">
      <label class="block text-sm font-bold text-gray-700 mb-2">Rechercher</label>
      <div class="relative">
        <%= f.text_field :search, placeholder: "Nom...", value: params[:search], class: "w-full h-12 px-4 pl-10 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-hero" %>
        <svg class="w-5 h-5 text-gray-400 absolute left-3 top-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
        </svg>
      </div>
    </div>

    <div class="w-full md:w-40 flex items-end">
      <%= f.submit "Filtrer", class: "w-full h-12 bg-blue-hero hover:bg-opacity-90 text-white px-6 rounded-lg font-bold inline-flex items-center justify-center" %>
    </div>
  </div>
<% end %>

<!-- Specialties Table -->
<div class="bg-white rounded-super shadow-sm overflow-hidden">
  <div class="p-6 border-b border-gray-200">
    <div class="flex items-center justify-between">
      <h2 class="text-2xl font-bold text-gray-900">Spécialités disponibles</h2>
      <%= link_to new_admin_specialty_path, class: "bg-blue-hero hover:bg-blue-dark text-white px-6 py-3 rounded-lg font-bold inline-flex items-center" do %>
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
        </svg>
        Ajouter spécialité
      <% end %>
    </div>
  </div>

  <div class="overflow-x-auto">
    <table class="w-full">
      <thead class="bg-soft">
        <tr>
          <th class="text-left px-6 py-4 text-sm font-bold text-gray-700">Spécialité</th>
          <th class="text-left px-6 py-4 text-sm font-bold text-gray-700">Actions</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-200">
        <% if @specialties.any? %>
          <% @specialties.each do |specialty| %>
            <tr class="hover:bg-soft transition">
              <td class="px-6 py-4">
                <div class="flex items-center space-x-3">
                  <% if specialty.icon.present? %>
                    <div class="w-10 h-10 bg-blue-hero rounded-lg flex items-center justify-center">
                      <span class="text-white text-lg"><%= specialty.icon %></span>
                    </div>
                  <% end %>
                  <div>
                    <%= link_to admin_specialty_path(specialty), class: "font-medium text-gray-900 hover:text-blue-hero" do %>
                      <%= specialty.name %>
                    <% end %>
                  </div>
                </div>
              </td>
              <td class="px-6 py-4">
                <div class="flex space-x-2">
                  <%= link_to admin_specialty_path(specialty), class: "text-blue-hero hover:text-blue-dark inline-flex items-center", title: "Voir" do %>
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                    </svg>
                  <% end %>
                  <%= link_to edit_admin_specialty_path(specialty), class: "text-blue-hero hover:text-blue-dark inline-flex items-center", title: "Modifier" do %>
                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                    </svg>
                  <% end %>
                  <%= button_to admin_specialty_path(specialty), method: :delete, form: { data: { turbo: false, controller: "confirm", confirm_message_value: "Êtes-vous sûr de vouloir supprimer cette spécialité ?", action: "submit->confirm#confirm" } }, class: "text-red-600 hover:text-red-700 inline-flex items-center", title: "Supprimer" do %>
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                    </svg>
                  <% end %>
                </div>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td colspan="2" class="px-6 py-8 text-center text-gray-500">
              Aucune spécialité trouvée
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <!-- Pagination -->
  <% if @pagy.pages > 1 %>
    <div class="p-6 bg-soft border-t border-gray-200 flex flex-col sm:flex-row justify-between items-center gap-3">
      <p class="text-xs sm:text-sm text-gray-600">
        Affichage de <strong><%= @pagy.from %></strong> à <strong><%= @pagy.to %></strong> sur <strong><%= @pagy.count %></strong> spécialité<%= @pagy.count > 1 ? 's' : '' %>
      </p>
      <%= pagy_nav_supercaralive(@pagy, params: request.query_parameters) %>
    </div>
  <% end %>
</div>

