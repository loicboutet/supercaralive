<div class="min-h-screen bg-soft py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-4xl mx-auto">
    <div class="bg-white rounded-super shadow-sm p-8 md:p-12">
      <!-- Header -->
      <div class="mb-8">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
          Conditions Générales d'Utilisation
        </h1>
        <% app_config = AppConfig.instance %>
        <% if app_config.terms_of_service.present? && app_config.updated_at.present? %>
          <p class="text-gray-600">
            Dernière mise à jour : <%= app_config.updated_at.strftime("%d/%m/%Y") %>
          </p>
        <% end %>
      </div>

      <!-- Content -->
      <% app_config = AppConfig.instance %>
      <% if app_config.terms_of_service.present? %>
        <div class="prose prose-lg max-w-none">
          <div class="whitespace-pre-wrap text-gray-700">
            <%= simple_format(app_config.terms_of_service) %>
          </div>
        </div>
      <% end %>

      <!-- Footer -->
      <div class="mt-12 pt-8 border-t border-gray-200">
        <p class="text-sm text-gray-600 text-center">
          En utilisant Supercaralive, vous reconnaissez avoir lu, compris et accepté les présentes Conditions Générales d'Utilisation.
        </p>
      </div>
    </div>
  </div>
</div>

